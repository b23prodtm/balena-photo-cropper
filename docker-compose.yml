version: '2.1'

services:
  cropper:
    build: ./cropper
    privileged: true
    restart: always
    ports:
      - "5000:5000"
    labels:
      io.balena.features.kernel-modules: '1'

  web:
    build: ./web
    depends_on:
      - cropper
    ports:
      - "80:80"
    restart: always
