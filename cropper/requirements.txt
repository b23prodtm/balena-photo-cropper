# requirements.txt - Fixed for armhf (Raspberry Pi) compatibility

# Flask and web dependencies
flask>=2.3.0
werkzeug>=2.3.0
gunicorn>=20.1.0

# Image processing - use versions with ARMv7 wheels
opencv-python-headless==4.8.1.78
numpy==1.26.4  # Last version with ARMv7 wheel support
Pillow>=9.5.0

# PDF processing - allow version range to find ARM-compatible binary
PyMuPDF>=1.22.0,<1.25.0

# HTTP client
requests>=2.28.0
